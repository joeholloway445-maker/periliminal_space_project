# Input prompts attribution

Kenney [Input Prompts](https://kenney.nl/assets/input-prompts) **CC0**.
Vendored subsets: `touch/` and `keyboard/` (Default PNG set only).
Lookup helper: `godot/src/ui/input_prompts.gd` (`InputPrompts` class).
